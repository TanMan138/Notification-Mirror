# Copy to Secrets.xcconfig and fill in your Supabase project. Do not commit Secrets.xcconfig (gitignored).
# xcconfig treats "//" as comment, so we use host only and build URL in app.
SUPABASE_HOST = YOUR_PROJECT_REF.supabase.co
SUPABASE_PUBLISHABLE_KEY = your_publishable_key_here
